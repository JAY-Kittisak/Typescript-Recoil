import { atom } from 'recoil';

export const logoAnimationState = atom({
    key: 'logoAnimationState',
    default: true
})
export const atomTattooState = atom({
    key: 'atomTattoo',
    default: true
})

//TODO Count
export const countState = atom({
  key: 'CountState',
  default: 1,
})
